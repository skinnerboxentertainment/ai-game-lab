{
  "name": "ai-game-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AI Game-Making Lab — production-track starter (Vite + TypeScript + PixiJS v8 + bitECS) with a deterministic pure-core, fixed-timestep sim, and a headless verify harness. See README.md and docs/AI_GAME_LAB_BLUEPRINT.md.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types tests/state.test.ts && node --experimental-strip-types tests/sandbox.test.ts && node --experimental-strip-types tests/harness.test.ts",
    "lint": "node scripts/lint.mjs",
    "verify": "node scripts/verify.mjs",
    "new-game": "node scripts/new-game.mjs",
    "check": "npm run test && npm run typecheck && npm run build && npm run lint",
    "preview": "vite preview"
  },
  "dependencies": {
    "bitecs": "^0.3.40",
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
